<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="005-add-embedding-columns" author="inboxai" dbms="postgresql">
        <sql>ALTER TABLE items
             ADD COLUMN embedding vector(512),
             ADD COLUMN embedded_at TIMESTAMP WITH TIME ZONE</sql>
        <sql>CREATE INDEX idx_items_embedding_cosine
             ON items USING hnsw (embedding vector_cosine_ops)</sql>
        <rollback>
            <sql>DROP INDEX IF EXISTS idx_items_embedding_cosine</sql>
            <sql>ALTER TABLE items
                 DROP COLUMN IF EXISTS embedding,
                 DROP COLUMN IF EXISTS embedded_at</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
